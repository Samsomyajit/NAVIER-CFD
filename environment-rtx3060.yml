name: navier-cfd
channels:
  - conda-forge
dependencies:
  - python=3.11
  - pip
  - numpy>=1.24
  - pip:
      - -e .
      # Install the CUDA-enabled PyTorch wheel separately after creating the environment.
